import Card from './Card';

export default class attackCard extends Card {
  constructor(cardInform) {
    super(cardInform);
  }

  useEffect() {
    // console.log('attack damage is' + value);
  }
}
